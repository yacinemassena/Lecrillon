====== FAST SETUP (Preprocessed Memmaps) ======

cd /workspace
git clone https://github.com/yacinemassena/Lecrillon.git
cd Lecrillon
bash setupvps.sh

# Install rclone (one-time)
curl https://rclone.org/install.sh | sudo bash

# Download preprocessed memmaps (~25 GB) — all you need for training
python download_data_fast.py --data-type preprocessed --transfers 32

# Also need raw VIX for target computation + stock for anchor dates
python download_data_fast.py --data-type vix --transfers 32
python download_data_fast.py --data-type stock --transfers 32

# Train — auto-detects preprocessed dir, ~10,000x faster loading
python train.py --seq-len 15000

====== FULL SETUP (All Raw Data) ======

cd /workspace
git clone https://github.com/yacinemassena/Lecrillon.git
cd Lecrillon
bash setupvps.sh

# Install rclone (one-time)
curl https://rclone.org/install.sh | sudo bash

# Download all data with parallel transfers
python download_data_fast.py --data-type all --transfers 32

# Train
python train.py --seq-len 15000 --batch-size 8 --n-layers 6 --d-model 256

====== EXAMPLE TRAINING COMMANDS ======

python train.py --seq-len 15000 --train-end 2024-03-30 --val-end 2024-11-30

python train.py --seq-len 15000 --batch-size 8 --n-layers 6 --d-model 256 --train-start 2024-01-01 --train-end 2024-03-30 --val-end 2024-11-30

# Explicit preprocessed path (if not auto-detected)
python train.py --seq-len 15000 --preprocessed-path datasets/preprocessed

====== LOCAL (WSL) ======

wsl
cd /mnt/d/Mamba\ v2
source venv/bin/activate

grad accum : 
# RTX 5080: batch=3 fits, simulate effective batch=12
python train.py --batch-size 3 --grad-accum 4
 
# A100: batch=16 fits, simulate effective batch=64
python train.py --batch-size 16 --grad-accum 4