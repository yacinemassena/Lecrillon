# TCN Pretraining for Multi-Stream VIX Prediction

Standalone TCN pretraining module for learning stream-specific representations from stocks, options, and index data. Pretrained encoders transfer to the main Mamba architecture.

## Overview

Train separate TCN-12 encoders per data stream (stocks, options, index) on SPY 30-day forward Realized Volatility prediction. Each stream learns specialized temporal patterns before integration into the full Mamba model.

## Quick Start

```bash
# Install dependencies
pip install -r requirementstcn.txt

# Precompute top stocks (one-time, for 16GB GPU filtering)
python scripts/compute_top_stocks.py

# Train stocks TCN on RTX 5080
python pretrain_tcn_rv.py --profile rtx5080 --stream stocks

# Train options TCN on H100
python pretrain_tcn_rv.py --profile h100 --stream options

# Train index TCN
python pretrain_tcn_rv.py --profile h100 --stream index
```

## Architecture

Per `architecture.md` specification, each stream uses a **stream-specific architecture**:

| Stream | Layers | Hidden Dim | Dropout | Parameters |
|--------|--------|------------|---------|------------|
| **Index** | 4 | 128 | 0.15 | ~600K |
| **Options** | 8 | 256 | 0.10 | ~4M |
| **Stocks** | 12 | 512 | 0.10 | ~18M |

- Auxiliary task: SPY 30d forward RV prediction (target only, not input)
- Transfer encoders to: `e_stk`, `e_opt`, `e_idx` in Mamba Level 0

## Data Streams

| Stream | Volume | Filtering | Batch Size (16GB) | Batch Size (80GB) |
|--------|--------|-----------|-------------------|-------------------|
| **Stocks** | ~8M ticks/day (filtered) | Top 100 by volume | 2,000 chunks | 11,600 chunks |
| **Options** | ~5.4M ticks/day | None | 2,800 chunks | 16,000 chunks |
| **Index** | ~347K ticks/day | None | 4,000 chunks | 24,000 chunks |

## GPU Profiles

- **rtx5080**: 16GB VRAM, stock filtering enabled
- **h100/a100**: 80GB VRAM, full universe

## Configuration

Edit `config_pretrain.py`:
- `STREAM_CONFIGS`: Per-stream paths, batch sizing, filtering
- `GPU_PROFILES`: VRAM-aware batch limits
- `PretrainConfig`: TCN architecture, training hyperparameters

## Outputs

Checkpoints saved per stream:
- `checkpoints/tcn_stocks_best.pt` - Full model
- `checkpoints/tcn_stocks_encoder.pt` - Encoder only (for transfer)
- `checkpoints/tcn_options_best.pt`
- `checkpoints/tcn_options_encoder.pt`
- `checkpoints/tcn_index_best.pt`
- `checkpoints/tcn_index_encoder.pt`

## Data Requirements

Expected directory structure:
```
datasets/
├── polygon_stock_trades/              # Stock tick data (2022-2023 subset)
├── options_trades/                    # Options tick data (2022-2023 subset)
├── index_data_2017_2025/values_v1/    # Index data: 1652 train files (2017-2022)
│                                      #              121 val files (Jan-Jun 2023)
│                                      #              Test files (Jul 2023-2025)
├── SPY_daily_rv/spy_daily_rv_30d.parquet  # Precomputed RV targets (5615 days)
└── top_100_stocks.txt                 # Generated by compute_top_stocks.py
```

**Dataset Coverage:**
- **Index stream**: Full 2017-2025 dataset (1773+ files)
- **Stocks/Options**: 2022-2023 subset (optimized for development)
- **Training split**: 2017-2022 (6 years)
- **Validation split**: Jan-Jun 2023 (6 months)
- **Test split**: Jul 2023-2025 (2.5 years)

## Validation

```bash
# Smoke test configuration
python scripts/validate_stream_config.py
```

## Features

- **Background prefetching**: Async file loading while GPU trains
- **On-the-fly filtering**: O(1) ticker lookup, no training stalls
- **Chunk-level batching**: Strict VRAM control
- **Stream-tuned batch sizing**: Optimized per data volume
- **Sample boundary tracking**: Proper loss computation across date boundaries

## Hyperparameter Search (Optuna)

Multi-GPU parallel hyperparameter search using Optuna:

```bash
# Search with 4 GPUs, 20 trials
python hyperparam_search.py --stream index --gpus 4 --n_trials 20

# Quick search (fewer epochs per trial)
python hyperparam_search.py --stream index --gpus 4 --n_trials 50 --epochs 10

# Resume previous search
python hyperparam_search.py --stream index --gpus 4 --n_trials 20 --resume
```

**Search spaces per stream:**

| Stream | Layers | Hidden Dim | LR Range | Dropout |
|--------|--------|------------|----------|---------|
| Index | 2-6 | 64-256 | 1e-5 to 1e-3 | 0.05-0.30 |
| Options | 4-10 | 128-512 | 1e-5 to 5e-4 | 0.05-0.25 |
| Stocks | 8-16 | 256-768 | 1e-5 to 3e-4 | 0.05-0.20 |

**Features:**
- **Parallel trials**: 1 GPU per trial, n_gpus trials run simultaneously
- **SQLite storage**: Resumable searches, results persist across runs
- **Early pruning**: Bad trials stopped early via MedianPruner
- **JSON output**: Best hyperparameters saved to `checkpoints/optuna/best_params_{stream}.json`

## VPS Setup (Flexible Data Handling)

The VPS setup automatically detects available data and adapts accordingly:

```bash
# 1. Run setup script (checks data availability)
bash setup_vps.sh

# 2. If data missing, download specific stream
python check_data_availability.py --download --stream index

# 3. Or download all available data from R2
python check_data_availability.py --download

# 4. Train on available stream
python pretrain_tcn_rv.py --stream index --profile h100
```

**Features:**
- ✓ **Graceful degradation**: Works with whatever data is available
- ✓ **Clear feedback**: Shows exactly what's missing
- ✓ **Selective download**: Download only what you need
- ✓ **Auto-detection**: No manual configuration required
- ✓ **Resume-friendly**: Can add data incrementally

**Data availability check:**
```bash
# Check what's available locally and on R2
python check_data_availability.py

# Output: available_streams.json
# {
#   "available_streams": ["index", "rv"],
#   "can_train": {
#     "index": true,
#     "stocks": false,
#     "options": false
#   },
#   "recommended_stream": "index"
# }
```

## Transfer to Mamba

Load pretrained encoders:
```python
# Load stocks encoder
ckpt = torch.load('checkpoints/tcn_stocks_encoder.pt')
frame_encoder.load_state_dict(ckpt['frame_encoder_state_dict'])
chunked_encoder.load_state_dict(ckpt['chunked_encoder_state_dict'])
```

Each stream's encoder slots into the corresponding position in Mamba Level 0.
